@model OmniConf.Web.ViewModels.Sessions.SessionsViewModel
@{
    ViewData["Title"] = "Home";
}
<h1>Sessions</h1>
@foreach (var session in Model.Sessions)
{
    <div>
        <h2>@session.Title</h2>
        <p>@session.Description</p>
    </div>
}